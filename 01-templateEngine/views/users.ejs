<%- include('includes/header.ejs') %>
    <h1>Users</h1>
    <% if (users.length> 0) { %>
        <ul>
            <% users.forEach((user)=> { %>
                <li>
                    <%= user.name %>
                </li>
                <% }); %>
        </ul>
        <% } else { %>
            <h3>No Users Found</h3>
            <% }%>

                <%- include('includes/footer.ejs') %>